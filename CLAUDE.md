# futary

ふたり専用SNS。「ふたりの毎日を、もっと特別に。」

## 復帰プロトコル（セッション開始直後・圧縮直後に必ず実行）

1. `docs/state.md` を読む — 現在地はここにしかない
2. 自分の役割（A / R / B）を確認する
3. 担当タスク `docs/tasks/NNN-*.md` を読む

会話履歴は失われる前提で動く。記憶はコンテキストではなくファイルに置く。

## 役割

| 役 | 実体 | 作業ディレクトリ | 担当 | 絶対にやらないこと |
|---|---|---|---|---|
| **A** 設計 | Claude Opus | `futary-A/` | 要件・設計・タスク作成・仕様変更判断 | 実装差分を読まない |
| **R** レビュー | Claude Opus | `futary-R/` | 実装レビュー・受け入れ判定 | 設計変更しない。書き込まない |
| **B** 実装 | **Codex GPT-5.6 Terra** | `futary/` | コード・テスト・動作証跡 | 自分の実装を自己採点しない |
| 監査 | **Codex GPT-5.6 Sol** | — | セキュリティ監査（読み取り専用） | コードを直さない |

**B は Codex。読む指示書は `AGENTS.md`**（ADR-012）。
`CLAUDE.md` と `AGENTS.md` を**食い違わせない**。片方を直したら他方も確認する。

**自分の作業ディレクトリの外でファイルを編集しない。**
役ごとに git worktree で分離してある。他の役のツリーに書くと分離の意味が消える。

### R（レビュー）として呼ばれたとき

B（Codex）は `claude -p` であなたを呼ぶ。以下に従う。

- **設計を変えない。** 仕様の問題を見つけたら A にエスカレーションする
- 差分は自分で取る（`git diff main...HEAD`）。渡された範囲だけを見ない
- **タスクファイルの確認観点と完了条件がそのまま採点表。** 独自の観点を発明しない。
  必要だと感じたらタスク定義が甘いということなので、指摘として挙げる
- **テストが「拒否されること」を検証しているかを最優先で見る。**
  成功パスだけでは、対象の制御を丸ごと削除しても緑のままになる
- 「動作に影響しない好み」を完了判定に使わない（記録のみ）
- 断定と推測を分ける。実行して確かめていないことは「未確認」と書く
- 返答の**先頭**に判定ブロックを置く。書式を崩さない

```
---
verdict: approved | change_requested | blocked
task: NNN
attempt: N
---
```

`attempt` が3に達しても `approved` にできない場合は `blocked`。4回目を求めない。

運用ルールの詳細は `docs/harness.md`。

## ファイル

| パス | 内容 |
|---|---|
| `docs/state.md` | **現在地（最重要）** |
| `docs/worklog.md` | 作業ログ（追記専用） |
| `docs/requirements.md` | 要件・スコープ外 |
| `docs/architecture.md` | 技術構成・データモデル・API・デザイントークン |
| `docs/conventions.md` | 規約・テスト方針・Git運用 |
| `docs/security-requirements.md` | 認証・機密・想定脅威 |
| `docs/decisions.md` | ADR（なぜその設計か） |
| `docs/tasks/NNN-*.md` | タスク定義 |
| `artifacts/NNN/` | 動作証跡 |

## 常に守る

- **ファイル変更を伴う作業の完了時は必ず `docs/state.md` を更新し、`docs/worklog.md` に追記する**
- `worklog.md` は追記のみ。既存行の編集・削除は禁止
- 秘密情報をコードに書かない。ローカルは `.dev.vars`、本番は `wrangler secret`
- 全ての手続きで `couple_id` スコープを強制する。他ペアのデータに触れる経路を作らない
- 1タスク完了ごとに `/clear` する（圧縮させない方が安い）
- ドキュメント・コードコメントは日本語
