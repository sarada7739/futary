# futary

ふたり専用SNS。「ふたりの毎日を、もっと特別に。」

## 復帰プロトコル（セッション開始直後・圧縮直後に必ず実行）

1. `docs/state.md` を読む — 現在地はここにしかない
2. 自分の役割（A / R / B）を確認する
3. 担当タスク `docs/tasks/NNN-*.md` を読む

会話履歴は失われる前提で動く。記憶はコンテキストではなくファイルに置く。

## 役割

| セッション | 担当 | 絶対にやらないこと |
|---|---|---|
| **A** 設計 | 要件・設計・タスク作成・仕様変更判断 | 実装差分を読まない |
| **R** レビュー | 実装レビュー・受け入れ判定 | 設計変更しない。書き込まない |
| **B** 実装 | コード・テスト・動作証跡 | 自分の実装を自己採点しない |

運用ルールの詳細は `docs/harness.md`。

## ファイル

| パス | 内容 |
|---|---|
| `docs/state.md` | **現在地（最重要）** |
| `docs/worklog.md` | 作業ログ（追記専用） |
| `docs/requirements.md` | 要件・スコープ外 |
| `docs/architecture.md` | 技術構成・データモデル・API・デザイントークン |
| `docs/conventions.md` | 規約・テスト方針・Git運用 |
| `docs/security-requirements.md` | 認証・機密・想定脅威 |
| `docs/decisions.md` | ADR（なぜその設計か） |
| `docs/tasks/NNN-*.md` | タスク定義 |
| `artifacts/NNN/` | 動作証跡 |

## 常に守る

- **ファイル変更を伴う作業の完了時は必ず `docs/state.md` を更新し、`docs/worklog.md` に追記する**
- `worklog.md` は追記のみ。既存行の編集・削除は禁止
- 秘密情報をコードに書かない。ローカルは `.dev.vars`、本番は `wrangler secret`
- 全ての手続きで `couple_id` スコープを強制する。他ペアのデータに触れる経路を作らない
- 1タスク完了ごとに `/clear` する（圧縮させない方が安い）
- ドキュメント・コードコメントは日本語
