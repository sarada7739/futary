# 018: カレンダーの改善 — 人間の実機確認（未実施）

カレンダー画面は認証必須のため、B（自動化）はこの経路を通れない
（011・013と同じ制約。conventions.md 8節）。

自動テスト（`apps/api/test/event.test.ts`・`apps/app/test/calendar-screen.test.tsx`。
`artifacts/018/test-results.md`参照）で担保しているのは契約・DB制約・画面結合の
配線までで、**実データ・実機での見た目・操作感は未確認。**

## 確認してほしい項目

1. `wrangler dev --remote` でログインし、リモートD1に`0008_event_time_and_meetup_unique.sql`
   を適用してから確認する（このタスクのマージ後に適用する。適用コマンドは
   `docs/worklog.md`の0007の記録と同じ形）
2. 予定・記念日・会った日それぞれに、設定した人の名前が添えて表示される
3. 予定・会った日に時間を任意で設定できる。記念日では時間の項目自体が出ない
4. 時間の有無で予定の行の高さが変わらない
5. **同じ日に「会った日」を2回登録すると、後で登録した方に上書きされ、
   上書きされたことが分かる注記が出る**（人間の要望どおりの挙動か）
6. 既に「会った日」がある日へ、編集で別の「会った日」を移動しようとすると
   保存できず、その理由が分かる
7. ホーム画面の統計カードの「会った回数」が、1日1件に揃った後も違和感のない
   数字になっているか（`meetupCount`の意味が実質「会った日数」になった点。
   `docs/requirements.md`の文言とは食い違わないとBは判断したが、実際の
   見え方も確認する）

## 進め方

条件を満たしたことにはしない。この節はチェックを付けず、`docs/state.md` の
未解決の論点に本タスクを起票する。人間が実機で確認し次第、この節に結果を追記する。
