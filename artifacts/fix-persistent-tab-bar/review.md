# fix/persistent-tab-bar — Rレビュー結果

Rのメッセージを一字一句そのまま保存する（conventions.md 8節）。

---

R です。**PR #105 受け入れ**。CI pass 1m47s 緑。マージして構いません。**実装への必須修正はありません。**ただし A に上げてほしい件が1件あります。

## `architecture.md` の中に矛盾する2つの記述があります

同じファイルの中で、**94行離れて逆のことが書かれています。**

| 行 | 節 | 記述 |
|---|---|---|
| 588 | タブに出すのは動く機能 | **カレンダーをタブにする。`アルバム` を置き換える** |
| 682 | **7節 ナビゲーション** | ボトムタブ5つ: ホーム / **アルバム** / ＋投稿 / 検索 / マイページ。**アルバム と 検索 は枠のみ** |

**7節は「ナビゲーション」という名前の節です。** タブ構成を調べる人が最初に開く場所に、古い方が残っています。しかも「タブを4つに減らすとデザインの印象が変わるため、枠は残す」という根拠まで残っており、これは今回の判断で上書きされたはずのものです。

A が新しい節を足したときに 7節を直していません。**これは 010 の `lib/date.ts`（うるう年の答えが2つ）や `architecture.md`（射影する年は最大2つ）と同じ形です。** 新しい記述を足しても、古い記述が消えなければ矛盾が残ります。

A に上げてください。**あなたのブランチでは直さないでください**（設計ドキュメントは A の領分。conventions.md 9節）。返答を待たずマージして構いません。

## 実装の確認結果

**新しいアイコンは既存4点と揃っています。** 実測しました。
```
tab-home      72x70  角A=0  不透明サンプル 306
tab-album     72x66  角A=0            332
tab-search    72x64  角A=0            205
tab-profile   69x72  角A=0            222
tab-calendar  72x72  角A=0            251   ← 新規
```
**透過が正しく、線の密度も既存の範囲内**（205〜332）です。浮きません。ラスタライズの手段が無い環境で canvas API に回した判断も妥当です。

`(tabs)/calendar` への移動、`tintColor` の維持、`compose.tsx` へのキャンセルボタン追加も確認しました。

## 1点、次回のために

タブアイコンは `TabIcon` で `tintColor` を当てるので、**元画像の線色は表示に影響しません。** 「線色も実測して揃えた」という作業は結果に出ていません。

効くのは**透過・形・線の太さ**の3つです。次に素材を作るときはそこに時間を使ってください。今回の成果物自体は問題ありません。

## 実機確認について

人間が M3 の受け入れで見つけた2点は、**どちらもテストでは出ないものでした。** 「タブが消えて戻れない」も「カレンダーがホームのボタンからしか行けない」も、画面遷移の構造と情報設計の話です。

「タブが消えること自体は `(tabs)` 配下に置けば expo-router が機械的に保証するので、回帰テストではなく実機確認に委ねる」という判断は妥当だと考えます。**ルーティングの構造をテストで固定しようとすると、フレームワークの内部を検査することになります。**

M3 受け入れの再試行を人間に依頼してください。前回報告された2点が解消しているかに加えて、私が挙げた優先順位（PC幅のホーム、カレンダー画面全般、3月末の思い出し、繰り返し記念日の削除）も残っています。
