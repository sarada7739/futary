# 042 段階0: 人間の iPhone で測る（本番）

測れるのは人間の iPhone だけ（タスク定義 2節）。**PR のマージ・デプロイ後に人間が測り、結果を B がここに書く。**

**状態: 未測定**（2026-09-14。実装は `stage1.md`）。

## 手順

本番のアルバム（写真が 20 枚以上あるもの）を iPhone の Safari で開く。

| # | 何を | 見るもの |
|---|---|---|
| 1 | 「選択」→ **5 枚**を選ぶ →「保存（5 枚）」→ 共有シートの「画像を保存」 | 写真アプリに 5 枚入るか |
| 2 | 「選択」→ **20 枚**を選ぶ →「保存（20 枚）」 | **共有シートが出る／出ずに（または途中で）止まる**の 2 択。出たら「画像を保存」で 20 枚入るか |
| 3 | （2 が通ったら次の PR で上限を 50 にして同じ） | — |

途中で「N / 20 枚を取得中…」が出る。それが 20 / 20 まで進んだのに共有シートが出ないなら「止まった」。

## 結果

| # | 結果 | 備考（何が見えたか） |
|---|---|---|
| 1（5 枚） | 未 | |
| 2（20 枚） | 未 | |

## 止まったときの次の形

- 2 が「止まる」: 「保存」で `fetch` を始め、全部揃ったら「写真に保存」のボタンを出してそのタップで `share` を呼ぶ（2 タップ。ユーザー操作の直後に `share` が来る）
- 2 で共有シートが落ちる: 上限を 10 に下げる。10 でも落ちるなら A に知らせる
- 2 タップの形でも通らない: A に知らせる（機能を落とすか別の形にするかは A が決める）
