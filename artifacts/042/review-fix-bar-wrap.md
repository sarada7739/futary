R から B へ。PR #308（fix/album-selection-bar-wrap、head f0bc484）は受け入れ。必須修正は無い。squash merge してよい。この文を `artifacts/042/review-fix-bar-wrap.md` に一字一句保存すること。

# PR #308 — R の判定

futary-R で f0bc484 を checkout して実行した。app 415・型チェック・lint 緑。

## 4 点への答え

1. **事象**: 人間の実機の観察で、私は再現していない（jsdom では折り返しを測れない。B の言うとおり）
2. **原因の計算は合っている**: `Button` は `paddingHorizontal: space.xl`（24）・`fontSize: 16`。390px 幅で `padding: space.lg`（16）× 2 と `gap: space.sm` × 2 を引いて 3 等分すると 1 つ約 113px、文字に使えるのは約 65px。「カバーにする」6 字 ≒ 96px は入らない。「カバー」3 字 ≒ 48px は入る
3. **再発を防ぐ手段**: ラベルの短縮（人間の決定）。回帰テストが書けない理由は正しい。テストは文言と「N 枚を選択中」がヘッダーにあることを見る形で、22 件緑。見た目の確認は人間の iPhone
4. **影響範囲**: 差分は `album-detail.tsx` のラベル 2 箇所とコメントだけ。タイムライン・ゲスト（「保存」だけの行）は幅いっぱいなので影響無し。API・契約・設計文書は触っていない（3節の「保存（N 枚）」は A が直す）

## 記録（判定に使わない）

- `Button` は `numberOfLines` を持たないので、将来 3 つ横並びの行を作ると同じ形で折れる。「3 つ横並びは作らない」か「`Button` に 1 行固定の口を持たせる」かは、次に同じ場面が出たときで足りる（今は B の言うとおり他に無い）

段階0（5 枚・20 枚）は未聴取のまま。
