# 022: 時刻の選択と、日付の8桁入力 — 人間の実機確認（未実施）

カレンダー・マイページ画面は認証必須のため、B（自動化）はこの経路を通れない
（011以降と同じ制約。conventions.md 8節）。

自動テスト（`apps/api/test/event.test.ts`・`schema-integrity.test.ts`・
`migration-existing-rows.test.ts`・`apps/app/test/calendar-screen.test.tsx`・
`time-wheel.test.ts`・`date-input8.test.ts`。`artifacts/022/test-results.md`
参照）で担保しているのは契約・DB制約・純粋関数の配線までで、**実機での
見た目・スクロール操作の感触・PC/スマホでの表示差は未確認。**

## 確認してほしい項目

1. 時刻の選択（時・分の2列ホイール）が、スマホ幅で押しやすいか
2. **iPhoneのSafariで、指を離したあと値が確定するか**（`scrollend`が無い
   ため、scrollの停止をタイマーで拾う実装にしている。ここが唯一の落とし穴
   とタスク定義に明記されている）
3. PCのSafari・Chromeでも同じ形（ホイールの見た目・上下のフェード）に
   見えるか。`<input type="time">`を採らず自前で作ったのはこのため
4. 既存の予定を開いたとき、入っていた時刻がそのまま出るか。特に5分刻みに
   乗らない時刻（例: `12:07`）を持つ予定を開いたとき、その値が選択肢の
   1行として出て、選択状態になっているか
5. 4の予定を、時刻に触れずタイトルだけ変えて保存したとき、時刻が
   （`12:05`などに丸められず）`12:07`のまま残るか
6. 開始時刻を設定する前は「終了時刻を追加」ボタンが出ない
   （開始を選ぶ前に終了を選べない。押せてから断られる形にしていない）
7. 終了を空にしたまま保存できるか
8. 終了を開始より前・同じ時刻にしようとすると、保存できない旨が分かるか
9. 日付の8桁入力（マイページの付き合った日・結婚した日、カレンダーの
   予定の日付）で、8桁を打つ途中で表示が壊れて見えないか
   （8桁そろうまではハイフンを入れない実装にしている）
10. 存在しない日付（`20240230`等）を打つと、保存できないと分かるか
11. カレンダーの一覧で、開始・終了の両方がある予定は「12:00〜13:00」の
    形で、開始だけの予定は「12:00」のまま読めるか

## 進め方

条件を満たしたことにはしない。この節はチェックを付けず、`docs/state.md` の
未解決の論点に本タスクを起票する。人間が実機で確認し次第、この節に結果を
追記する。
