# 035: 見た目を作り込む — 人間の実機確認項目

Bが自動で確認できない項目をここに列挙する。実装が進むごとに追記する。

## フォント（035書体仕様）

- **iPhoneでホーム画面に追加したとき、アプリのアイコンが正しく出るか**
  （030で`apple-touch-icon.png`を`+html.tsx`に追加したが、本番で実際に
  効いているかはまだ誰も確かめていない。ローカルの`expo start --web`では
  `${baseUrl}`〈"/app"〉付きのURLが404になることを実測したが〈本番の
  静的書き出しでは`${baseUrl}`が実際の配置と一致するはず〉、本番での
  200応答・実機での見え方はBからは確認できない。Aの指摘で追加した項目）
