# 048 fix（PR #342）— R の判定

futary-R で bb0e2b5 を checkout して実行した。app 512 緑・型・lint 緑。CI pass。差分は `lib/album-zip.ts`（`safeZipName`・`captionsText` の 2 関数）と `test/album-zip.test.ts` だけ。触ったものは戻した。

**判定: 受け入れ。必須修正なし。**

## 採点表（PR 本文の 4 点）

1. 観測した事象: 段階1の R の記録 1・2 と一致
2. 原因: `safeZipName` の文字集合と `captionsText` の TAB、検査の中身が縮まないバイト列。読んで一致
3. 再発を防ぐ手段: Z1b 3 本と Z1 の作り直し。下の壊しで全部赤
4. 影響範囲: 2 関数だけ。`/` → `_` などの既存の名前は変わらない（既存の名前のテストは緑のまま）

## 壊して確かめたこと（`album-zip.test.ts` 20 本）

| 壊し方 | 赤 |
|---|---|
| `.` だけの名前を `album` に倒すのを外す | 2 本（Z1b） |
| 制御文字・TAB・改行を名前から落とさない | Z1b 2 本 + 既存 1 本 |
| `zipSync` の `level: 0` を外す（圧縮する） | 1 本（Z1。段階1で緑だったもの） |
| 説明文の TAB を空白にしない | 2 本（Z1b） |

## 読んで確かめたこと

- A が 2節に足した定義（「安全な文字: `/ \ : * ? " < > |` と制御文字・TAB・改行を `_`」「`.` だけの名前は `album`」「Z1 は縮む中身でも ZIP ≥ 中身の合計」「Z1b」）と実装・テストが一致
- `..a` は `..a` のまま（`.` だけではない。定義どおり）。`"  ..  "` は trim のあとに判定するので `album`
- `UNSAFE_NAME_CHARS` の `eslint-disable-next-line no-control-regex` は理由付き。lint 緑
- Z1 は `payload > 3 × 4KB` を先に見てから `ZIP ≥ payload`（検査そのものが空回りしない形）

## 記録（判定に使わない）

- 無し

## 私が確かめていないこと

- 人間の実機（段階1と同じ。iPhone・PC で 1 アルバムと全部・100 枚）
