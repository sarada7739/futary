# 013: 思い出し — 人間の実機確認（未実施）

ホーム画面は認証必須のため、B（自動化）はこの経路を通れない
（003・004・007・011・012と同じ制約。conventions.md 8節）。

自動テスト（`apps/api/test/memory.test.ts`・`apps/app/test/memory-card.test.tsx`。
`artifacts/013/test-results.md`参照）で担保しているのは探索ロジックと
oRPCクライアントをモックした画面結合の配線までで、**実データ・実機での
見た目は未確認。**

**013の完了はM3全体の完了を意味する**（タスク定義の停止条件）。ここに
これまで溜まっていた実機確認項目（L59・017・L62・R-37・L68）もまとめる。

## 013固有の確認項目

1. 実機でカードのレイアウト・画像表示がデザインサンプル（「去年の今日」画面）
   に近い見た目になっているか
2. 実データで「1ヶ月前」「半年前」「1年前」「あの日の思い出」の切り替わりが
   自然に見えるか
3. **月末クランプ（L61）がここで初めて利用者から見える。** 3/29・30・31頃に
   同じ投稿が複数日続けて「1ヶ月前の今日」として出ること（不具合ではない）を
   確認できるタイミングがあれば見てほしい（急ぎではない）
4. 同じ日に開き直しても同じ思い出が出るか
5. 画像タップで全画面表示が開くか（「元の投稿へ遷移」をこう読み替えた設計。
   `docs/tasks/013-memory.md`実装メモ参照）

## M3で溜まっている項目（まとめて回収）

### L59・017（画像の全画面表示・画面の最大幅）
- PC幅でのホーム画面（640pxのコンテンツ列と全幅のタブバーの同居）
- 横長・縦長写真での全画面表示（`contain`のレターボックスの出方）
- 画像のどこをタップしても閉じること
- Android実機の戻るボタンで全画面表示が閉じること

### L62（011: カレンダーUI）
- 前月・翌月ナビゲーション
- イベントの登録・編集・削除がD1に実際に反映されるか
- 種別マーカーの色の見分けやすさ
- 繰り返し記念日の実データでの表示

### R-37（011: 繰り返し記念日の削除）
- 繰り返し記念日を削除したとき、他の年からも消えることが分かるか

### L68（012: 統計カード）
- カードのレイアウト（デザインサンプルとの近さ）
- 「あと○日」表示の自然さ
- 「招待中」表示の分かりやすさ

## 進め方

条件を満たしたことにはしない。この節はチェックを付けず、`docs/state.md` の
未解決の論点に本タスクを起票する。**M3全体の受け入れ判定として、人間が実機で
上記すべてを一度に確認する**（conventions.md 8節の手順。013の停止条件
「この時点でM3完了。人間による受け入れ判定を行う」に対応）。
