# 013: 思い出し — Rレビュー結果

Rのメッセージを一字一句そのまま保存する（conventions.md 8節）。

---

R です。**013 受け入れ**。CI pass 2m12s 緑。マージして構いません。**必須修正はありません。M3 の実装が揃いました。**

## 穴が構造的に塞がっています

007 で入れた制約が生きていることを確認しました。
```js
if (trimmedBody === "" && !input.imageId) throw errors.INVALID_INPUT();
```

**すべての投稿は本文か画像のどちらかを必ず持ちます。** したがって：

- 画像あり → 画像タップで全画面表示
- 本文あり → 本文タップで展開
- **両方なし → 存在し得ない**

**タップ先が無い思い出は作れません。** 個別に対処を足したのではなく、既存の制約と組み合わさって網羅されています。「本文があれば常に展開できる」という判断（省略判定を持たない）が、たまたまではなくこの網羅性を成立させています。

コメントに「探索4段目のランダム選択は画像を優先しないため起こりうる」と**なぜこの穴が生まれたか**まで書いてあるのも良い。次の人が「画像は必ずあるのでは」と考えて外す余地を潰しています。

## 記録1件（次に A がこのファイルを触るときで構いません）

仕様と実装で文言がずれています。

- タスク定義（A）: 「本文が**省略されているとき**、カードのタップで展開する」
- 実装（B）: 本文があれば**常に**展開できる

あなたの理由（省略の判定は当たり判定・計測に依存するので持たない）は妥当です。**017 で当たり判定に依存して穴を空けた教訓の応用**として筋が通っています。

であれば**仕様側の文言を実装に合わせる**のが筋です。今のままだと「省略判定が実装されている」と読めます。次に触る人が「判定が抜けている」と考えて足す余地があります。A に一報しておいてください。

## M3 の締めについて

マージ後、人間への受け入れ依頼に進んでください。`artifacts/013/manual-check.md` に L59・017・L62・R-37・L68 をまとめてある件、確認しました。

依頼の際、**確認項目に優先順位を付けてください。** 数が多いので、全部同じ重みで並べると人間の負担が大きい。私の見立てでは以下が上位です。

1. **PC 幅でのホーム画面**（L59）— テストは「640px が適用されている」ことしか言えません。窮屈でないかは人間にしか分かりません
2. **カレンダー画面全般**（L62）— B も私も一度も見ていない画面です
3. **3月末の思い出し**（L61 の可視化）— 同じ思い出が3日続く挙動。規則としては正しいが、体感が許容できるかは別
4. **繰り返し記念日の削除**（R-37）— 全年から消えることが伝わるか

Android の戻るボタンは端末が要るので、無ければ後回しで構いません。

M3 が受け入れられれば次は M4（014 ゲストデモ・015 LP・016 公開）です。014 は `security-requirements.md` 10節の監査必須枠（デモ経路＝T4・T5）なので、また生の監査出力との突き合わせを行います。
