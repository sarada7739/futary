# 047: プレミアムをやめたあと、無料枠を超える写真に鍵を掛ける

## 目的

**人間の指示。プレミアムをやめたペアが 100 枚持っていたら、無料枠（30 枚）を超える分は取り上げる。取り上げる前に ZIP で持ち出せる（048 段階1）。**

**「取り上げる」= 消すのではなく鍵を掛ける**（人間の絵 `プレミアム/03`: ぼかして「プレミアムで解放」）。
もう一度プレミアムにすれば戻る。実体は R2 に残る。

## 0. 先に決めたこと（A）

| # | 論点 | 決定 | 理由 |
|---|---|---|---|
| 1 | いつ鍵が掛かるか | **free に戻ってから 30 日の猶予のあと**（`couple_plans.expires_at` から 30 日。手で free にしたときは `updated_at` から 30 日） | 持ち出す時間。決済（048）の「期間終了まで有効」の上に 30 日乗る |
| 2 | どの写真に鍵が掛かるか | **`taken_at` の古い順に 30 枚が残り、それより新しい分に鍵**（ペア全体。アルバムをまたいで数える） | 絵 `03` の並び（前が見えて後ろが鍵）。「最初に入れた 30 枚は無料」と一致する |
| 3 | 鍵の掛かった写真はどう見えるか | **一覧のマスに、ぼかした絵と鍵と「プレミアムで解放」**（絵 `03`）。押すと 045 のシート（`/premium` へ）。**ビューアで開けない・保存できない・ZIP に入らない** | 取り上げるとはそういうこと。ぼかしは**サーバが返す署名付き URL を出さない**ことで作る（画面で `blur` を掛けない。URL があれば取れる） |
| 4 | 鍵は行に持つか、読むときに計算するか | **読むときに計算する。**`album_photos` に列を足さない | 「free かつ猶予が過ぎた」は `couple_plans` だけで決まる。写真の行を書き換えると、戻すときにも書き換えが要る |
| 5 | 猶予の間の見え方 | **全部見える・保存できる・ZIP できる（048 段階1）。**一覧とマイページに**「○月○日までに保存してください。それ以降、無料枠を超える写真は見られなくなります」**の帯 | 持ち出しの猶予だと分かる形 |
| 6 | 鍵の間に削除して 30 枚未満にしたら | 鍵が外れる（読むときに計算するので自然にそうなる） | 「自分で減らす」道を残す |
| 7 | 鍵の間に足せるか | **足せない**（045 の `PLAN_LIMIT`。`used` は鍵の分も数える） | 鍵の分を数えないと、鍵の上にさらに 30 枚積める |
| 8 | Cron・削除のジョブ | **持たない。**全部読むときの計算 | 消さないのでジョブが要らない。壊れる経路が 1 つ減る |
| 9 | 実体（R2）の費用 | 持ち続ける。鍵の分も消さない | 100 枚で数十 MB。「戻れる」価値の方が大きい |
| 10 | リリース履歴 | 入れない | 制限 |
| 11 | 猶予の起点 | **`expires_at` があればそれ**（Stripe の `canceled` は `plan='free'` で `expires_at` を残す〈048〉。期限切れで free になった行も同じ）。**`expires_at` が無い free の行**は、`source='manual'` なら `updated_at`（運営が手で free にした）、`source='stripe'` なら **鍵なし**（Checkout を作るときに先に書く `plan='free'` の行。一度も paid になっていない〈048〉） | 払っていないペアに鍵を掛けない |
| 12 | 法務ページ・LP の文言 | **このタスクで戻す・直す**: `apps/landing/tokushoho.html` の「解約後のデータについて」を草案 `docs/legal/tokushoho-draft.md` の行の文面に。`apps/landing/terms.html` 8 節に草案 `terms-draft.md` の「30 日の猶予」の 1 行を足す。`index.html` の FAQ「写真はいつでも ZIP でまとめて持ち出せます」→「写真は ZIP でまとめて持ち出せます」（鍵の写真は ZIP に入らない） | 公開している文言と実装を同じにする（草案のメモ） |
| 13 | 鍵の判定の形 | **「鍵でない 30 枚の id」を引く**（`unlockedPhotoIds(coupleId)`。`taken_at, id` 昇順の先頭 30）。鍵かどうかは「この 30 に無い」。鍵の側の id の集合は作らない | 055 で 1 ペア 1,000 件 × 500 枚になり、鍵の側は数十万になりうる。30 は D1 の 1 文 100 パラメータ（`architecture.md` 4節）に収まる |
| 14 | 045 の部品の文言 | **「無制限」を消す**: `plan-limit-sheet.tsx`「写真枚数 無制限」→「写真 50 万枚まで」・`quota-warning-card.tsx`「プレミアムで無制限に」→「プレミアムで 50 万枚まで」・`usage-card.tsx`「プレミアムで無制限に ›」（`accessibilityLabel` も）→「プレミアムで 50 万枚まで ›」。045 の定義も同じ文言に直した | 鍵のマスを押すと出るのがこのシート。特商法・LP・`/premium` の「50 万枚まで」と同じ数字にする |
| 15 | `unlockedPhotos` の `albums.deleted_at IS NULL` | **残す**。`album.delete` は写真の行を物理削除するので今は効かないが、削除の形が変わっても 30 の枠を削除済みが食わない。**テストを 1 本**（削除済みアルバムに写真の行を直接置き、先頭 30 に入らないこと。T12） | 条件を落として「効かない」と「要らない」を混ぜない |

## 1. 判定（サーバ）

`lib/plan.ts` の `resolvePlan(row, now)` を広げて **`PlanState`** を返す:

```
{ plan: "paid" }
{ plan: "free", lockAt: null }              -- 行が無い・一度も paid になっていない（0節 #11）
{ plan: "free", lockAt: number }            -- paid だった。lockAt = 期限（または updated_at）+ 30 日。now < lockAt なら猶予中
{ plan: "free", lockAt: number, locked: true }   -- now >= lockAt。無料枠を超える分に鍵
```

- **鍵の対象**: ペアの未削除アルバムの `album_photos` を `taken_at, id` の昇順に並べ、**31 枚目以降**
- `photo.list`（アルバム）: 鍵の写真は **`url` を `null`**、`locked: true` で返す（`width`/`height`/`takenAt` はそのまま。マスの形を保つ）。**caption も返さない**
- `photo.downloadUrl`: 鍵の写真は `NOT_FOUND`（存在を教えない形に寄せる。`want.*` と同じ）
- `album.get` / `album.list`: `photoCount` は鍵を含む数。**`cover` が鍵の写真なら、鍵でない中でいちばん新しいものに倒す**（無ければ `null`）
- `couple.get`: `planState`（上の形）を返す。`albumQuota.used` は鍵を含む
- **判定は 1 箇所**（`lib/plan.ts`）。`photo.list` は「この写真が鍵か」を `unlockedPhotoIds(coupleId)`（0節 #13。先頭 30 の id）の 1 関数から引き、**`locked` のときだけ**使う（paid・猶予中は引かない）

## 2. 画面

| 場所 | 猶予中（`lockAt` あり・未到達） | 鍵の後（`locked`） |
|---|---|---|
| アルバム一覧 | 使用量のカードの上に帯: 「**9月30日までに**写真を保存してください。それ以降、無料枠を超える写真は見られなくなります」+「ZIP で保存」（048 段階1）+「プレミアムについて」 | 帯: 「無料枠を超える 70 枚は見られません」+「プレミアムについて」 |
| アルバム詳細 | 同じ帯（短く） | 鍵のマス（絵 `03`）。押すと 045 のシート |
| マイページ | 同じ帯 | 同じ |
| ビューア | — | 鍵の写真はビューアに渡さない（`url` が `null` のものは開かない） |

- 日付は `packages/date` の `formatDateJa`
- 鍵のマスの絵: `surface-tint` の四角 + 鍵のアイコン + 「プレミアムで解放」。**元の写真をぼかして出さない**（URL を返していないので出せない。絵はぼかしているが、それは絵の都合）

## 3. やらないもの

| | 扱い |
|---|---|
| 実体の削除・Cron | しない（0節 #8・#9） |
| 鍵の写真の選び方を利用者に選ばせる | しない。古い順に 30 枚（0節 #2）。減らしたければ削除 |
| 猶予の通知（メール・プッシュ） | しない（`requirements.md` 5節）。帯で出す |
| タイムライン（投稿の写真）への鍵 | しない。投稿は制限しない |

## 4. テストで証明すること

| # | 何を | どこで |
|---|---|---|
| T1 | `resolvePlan`: 行なし → free/lockAt null。**`plan='free', source='stripe', expires_at NULL`（Checkout 前の行）→ lockAt null**。paid 期限内 → paid。期限切れ +29 日 → free 猶予中。+30 日 → locked。Stripe の `canceled`（`plan='free'`・`expires_at` あり）も同じ起点。手で free（`source='manual'`・`updated_at`）も同じ | `apps/api` |
| T2 | locked で 40 枚: `photo.list` の 31〜40 枚目（`taken_at` 昇順）が `url: null, locked: true`、caption 無し。30 枚目までは URL あり。アルバムをまたいで数える | `apps/api` |
| T3 | locked で鍵の写真の `photo.downloadUrl` → `NOT_FOUND`。鍵でない写真は取れる | `apps/api` |
| T4 | locked でカバーが鍵の写真 → 鍵でない中でいちばん新しいものに倒れる | `apps/api` |
| T5 | locked で 10 枚消して 30 枚になる → 鍵が全部外れる。paid に戻す → 外れる | `apps/api` |
| T6 | locked で `addPhotos` → `PLAN_LIMIT`（`used` が鍵を含む） | `apps/api` |
| T7 | 猶予中は全部の URL が返る。`couple.get` の `planState.lockAt` が正しい | `apps/api` |
| T8 | 画面: 猶予中の帯に日付と「ZIP で保存」。locked で鍵のマスが出て、押すとシート。鍵のマスはビューアを開かない | `apps/app` |
| T9 | 別ペアの写真を数えない（`couple_id` スコープ） | `apps/api` |
| T10 | `/tokushoho` の「解約後のデータについて」が草案の文面（「30 日の猶予」を含む）。`/terms` 8 節に「30 日の猶予」の行。`/` の FAQ に「いつでも ZIP」が無い | `apps/api` |
| T11 | locked で 1,000 件 × 数十枚のペアでも `photo.list` の 1 ページが D1 の 1 文に収まる（`unlockedPhotoIds` は 30 個。鍵の側を IN に入れない） | `apps/api` |
| T12 | 削除済みアルバム（`deleted_at` あり）の写真の行が `unlockedPhotos` の先頭 30 に入らない | `apps/api` |

## 確認観点

- 運営の SQL で `expires_at` を過去にして猶予 → 鍵の順に見える（人間のペアで 1 度だけ。終わったら paid に戻す）
- 鍵の写真は URL を持たない（ネットワークの応答で確かめる）

## 完了条件

- T1〜T9 が緑。`pnpm -r test`・型チェック・lint
- スクリーンショット（両モード × 猶予の帯・鍵のマス）
- `state.md` / `worklog.md`

## 停止条件

- 先頭 30 の id を引く SQL が 1 文で書けない → 結果に書いて進む（先頭 30 は `ORDER BY taken_at, id LIMIT 30` の 1 文で済むはず。全件を読む形にはしない〈0節 #13〉）
- 「猶予 30 日」「古い順 30 枚」を変えたくなる → A へ（数字と並びは人間の判断が要る）
- 猶予の起点が 0節 #11 のどれにも当てはまらない行の形を見つけた → A へ（鍵を掛けずに進める）

## 順序

**054（LP）の後。**048 の本番の鍵への差し替え（人間の Stripe 準備）とは独立に進められる。iOS・メール認証の前。
