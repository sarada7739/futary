# 037: AIまとめ

## 目的

**その月の投稿を、LLM に短くまとめさせる。**

人間の指示。**ホームの「AIまとめ」パネルは 020 で置いてある**（`iconPanelAi`・COMING SOON）。
**027・029 と同じで、そこに `onPress` を付ける作業である。**

**「AIまとめを使用する」にチェックを入れた人だけ使える。**
**OpenAI のキーを使う予定だが、Claude API のキーでも動くようにする。**

## 0. 前提が1つ変わる

`requirements.md` 5節は、これを**「LLM API コストが無料運用と衝突する」**として
スコープ外に置いていた。

**その理由は消えていない。**費用は運営（＝人間）が持つ。
**だから歯止めを設計に入れる**（5節）。

**そしてオプトインの理由は費用ではない。**下記。

## 1. オプトインの意味は「外部へ送ること」への同意

**AIまとめは、投稿本文を OpenAI か Anthropic に送る。**

ADR-012 で**「運営は全部読める。対策していない」**と決めた。
**それは運営の話である。外部の第三者へ渡すのは別の話である。**

**チェックが要るのは、費用のためではなく、そこへの同意のためである。**

### 2人とも同意していないと使えない

**投稿はふたりのものである。**

**片方の同意で、相手の投稿を外部へ出さない。**

- `couple_members` に **`ai_opt_in`** を持たせる（**個人ごと**）
- **2人とも 1 のときだけ**生成できる
- **1人のペアでは使えない**（相手がまだ居ないので同意が取れない）
- **相手が同意していないとき、画面にそう出す。**「相手の同意を待っています」

**ADR-012 の「身内だけ」と同じ論法である。前提が揃っていないなら、動かさない。**

## 2. キーは運営が持つ。利用者に入れさせない

**`wrangler secret`。**ローカルは `.dev.vars`。

**利用者に自分の API キーを入れさせない。**

**ADR-012 で「運営は全部読める」と決めている。**
**利用者のキーを預かると、運営がそれを読めることになる。**
**自分のデータを読まれるのと、自分の課金に使えるキーを読まれるのは、重さが違う。**

## 3. プロバイダを切り替えられるようにする

| 環境変数 | 中身 |
|---|---|
| `AI_PROVIDER` | `openai` または `anthropic` |
| `OPENAI_API_KEY` | OpenAI のキー |
| `ANTHROPIC_API_KEY` | Anthropic のキー |

- **`AI_PROVIDER` が指すプロバイダのキーが無ければ、起動時に落とす**
  （`BETTER_AUTH_SECRET` と同じ fail-closed。`architecture.md` 8節）
- **`apps/api/src/lib/ai.ts` に窓口を1つ作る。**
  手続きからはプロバイダが見えない形にする
- **両方のキーを同時に読まない。**`AI_PROVIDER` で決まる方だけ

**モデル名も環境変数にしない。**`lib/ai.ts` の中に、プロバイダごとの既定を書く。
**環境変数を増やすほど、本番とローカルがずれる経路が増える。**

## 4. 月ごとにする。週次は作らない

`requirements.md` は「週次・月次」と書いていたが、**まず月次だけ。**

**週の投稿は少なすぎて、まとめる意味が出ない。**
**月ごとなら「思い出」として意味が出る。**

**週次を足すかは、月次が使われてから決める。**

## 5. 費用の歯止め（設計に入れる）

| | |
|---|---|
| **生成の回数** | **1ペア・1ヶ月あたり3回まで。**作り直しの余地は残す |
| **入力の長さ** | **その月の投稿本文の合計を 8000 文字で切る。**超えたら**古い方から落とす** |
| **画像を送らない** | **本文だけ。**画像は外部へ出さない |
| **生成した結果は保存する** | 開くたびに叩かない |
| **投稿が3件未満の月は生成しない** | まとめる中身が無い |

**「押すたびに課金される」形にしない。**

## 6. データモデル

```
couple_members
  ...
  ai_opt_in  INTEGER NOT NULL DEFAULT 0   -- 037。個人ごとの同意

ai_summaries                              -- 037
  couple_id   TEXT    NOT NULL -> couples.id
  month       TEXT    NOT NULL            -- YYYY-MM（JST）
  body        TEXT    NOT NULL
  provider    TEXT    NOT NULL            -- 'openai' | 'anthropic'
  model       TEXT    NOT NULL
  generated_count INTEGER NOT NULL DEFAULT 1  -- 5節の回数の歯止め
  created_at  INTEGER NOT NULL
  updated_at  INTEGER NOT NULL
  PRIMARY KEY (couple_id, month)
  CONSTRAINT ai_summaries_provider_check CHECK (provider IN ('openai','anthropic'))
```

- **`provider` と `model` を残す。**あとで「どのモデルが書いたか」が分からなくなる
- **CHECK に名前を付ける**（`architecture.md` 4節）
- `couple_members` は**他表から FK 参照されない**ので `ALTER TABLE ADD COLUMN` が通る
  （**実測してから書くこと**）

### `me.delete` に `ai_summaries` の削除文を足す

**4回目である**（027 `wishes` / 029 `moods` / 031 `post_images` / 037 `ai_summaries`）。

**`ai_summaries` は `couples` を参照する側。D1 は FK を常に強制する。**
**足さないと、まとめを1件でも持つペアはアカウント削除が恒久的に失敗する。**

**032 で入れた「残ってよい行以外は0件」の走査に、これは自動で映る。**
**映ることを確かめること。**

## 7. 契約

```
me.get              -> ... aiOptIn: boolean, partnerAiOptIn: boolean を足す
me.setAiOptIn       { optIn: boolean } -> { aiOptIn }
                    自分の分だけ。user_id を引数に取らない（029 と同じ）
aiSummary.get       { month } -> { body, provider, model, updatedAt, generatedCount } | null
aiSummary.generate  { month } -> 生成して保存し、同じ形を返す
                    2人とも同意していなければ FORBIDDEN
                    投稿が3件未満なら INVALID_INPUT
                    4回目は LIMIT_REACHED
```

- **`month` は `YYYY-MM`。未来の月は拒む**
- **T9 の対象。**`aiSummary.get` のキャッシュキーに `viewerKey` を含める

## 8. 出力を信用しない

**投稿本文は利用者が書く。それが LLM への入力に入る。**

**LLM の出力は、表示するだけにする。**

- **リンクにしない。**出力の中の URL を押せる形にしない
- **マークダウンを解釈しない。**素のテキストとして出す
- **出力から何かを実行しない**

**プロンプトインジェクションで書き換えられても、表示が変になるだけで済む形にする。**

**入力に入れるのは本文だけ。**利用者名・メールアドレス・画像・IDを入れない。

## 9. 画面

**`(tabs)/ai-summary.tsx`。**ボトムタブには出さない（027・029 と同じ）。

- ホームの「AIまとめ」パネルから `router.push("/ai-summary")`
- **月を選ぶ**（既定は先月。**今月はまだ終わっていない**）
- **同意していないとき**: チェックを入れる導線。**「投稿の本文が外部の生成AIに送られます」と書く**
- **相手が同意していないとき**: 「相手の同意を待っています」。**押せるボタンを出さない**
- **まだ生成していない月**: 「まとめを作る」ボタン
- **生成済み**: 本文と、`provider` / `model` / 更新日時
- **3回使い切ったとき**: そう出す
- 3状態（読み込み中 / 空 / エラー）

**マイページに同意のチェックを置く。**画面の中に埋めない。
**同意は設定であって、機能の一部ではない。**

## 10. デモ

**デモではまとめを生成しない。**

- **ゲストは `generate` を通れない**（サーバ側で拒む）
- **デモ用のまとめを1件シードに入れる**（`provider: 'openai'` として。**実際には叩かない**）
- **`artifacts/037/` に「シードのまとめは生成されたものではない」と書く**

## テストで証明すること
- **2人とも同意していないと `generate` が FORBIDDEN**（片方だけ・両方無し の両方）
- **1人のペアでは通らない**
- **ゲストは通らない**
- **4回目が `LIMIT_REACHED`**
- **投稿3件未満の月が `INVALID_INPUT`**
- **未来の月が拒まれる**
- **入力が 8000 文字で切られ、古い方から落ちている**
- **`AI_PROVIDER` が指すキーが無いと起動時に落ちる**
- **`AI_PROVIDER` を切り替えると、呼ばれる先が変わる**（プロバイダは差し替えて確かめる。
  **本物のAPIをテストで叩かない**）
- **`me.delete` が `ai_summaries` を消す**（032 の走査に映ることも）
- **画像・利用者名・IDが入力に入っていない**
- キャッシュキーに閲覧者が含まれる（T9）

## 確認観点
- **同意していない状態で、何が見えるか**
- **相手が同意していないとき、押せるボタンが出ていないか**
- **同意の文言が、何が起きるかを書いているか**（「外部の生成AIに送られます」）
- 生成に失敗したとき、**回数が減っていないか**

## 完了条件
- [ ] ホームの「AIまとめ」パネルから入れる（COMING SOON が消える）
- [ ] 2人とも同意したときだけ生成できる
- [ ] `AI_PROVIDER` で OpenAI と Anthropic を切り替えられる
- [ ] 歯止め（回数・長さ・件数）が全部効いている
- [ ] 上記のテストが緑
- [ ] `security-auditor` の監査で High 以上がゼロ
- [ ] `artifacts/037/` に証跡と `manual-check.md` を保存

## 停止条件
- 完了: 上記をすべて満たす
- **中断: 実際に API を叩く必要が出たら、叩く前に A に上げる**（費用が発生する）
- 中断: レビュー往復が3回を超えた場合、`docs/state.md` に論点を記載して A へ

## 進捗
- [ ] マイグレーション（`couple_members` の ADD COLUMN を実測してから）
- [ ] `me.delete` への追加と、032 の走査に映ることの確認
- [ ] `lib/ai.ts`（プロバイダの窓口）
- [ ] `aiSummary.*` と歯止め
- [ ] 画面とマイページの同意
- [ ] デモシード
- [ ] `security-auditor`
- [ ] 証跡保存 → `state.md` 更新 → `worklog.md` 追記
