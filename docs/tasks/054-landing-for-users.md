# 054: ランディングページを一般向けに作り直す

## 目的

**人間の指示（2026-09-16）。`https://nisoine.com/` を、使う人（ふたり）に向けたページにする。**
015 の LP は「デモ入口 + 技術構成（転職アピール）」だった。技術構成は `/tech` に移し、
`/` は Nisoine で何ができるかと、はじめる入口だけにする。

- 絵: `docs/sample/landing/mockup-full.jpg`（人間の絵。全体の構成と雰囲気の見本）
- 写真: `docs/sample/landing/`（10 枚。人間が用意。**すべて AI 生成。実在の人物・著作物ではない**。`docs/sample/README.md`）
- **絵にあって Nisoine に無いものは載せない**（043 の規則）: 「1 日 3 回の質問」「1,300 問のデッキ」「ブラインド回答」「みんなの声」「Google Play のレビュー」「写真 30GB」「通報届け出済み」「レビュー」のナビ。**文言は 3 節に全部書く。B は 3 節の外の売り文句を足さない**

## 0. 決めたこと（A）

| # | 論点 | 決定 | 理由 |
|---|---|---|---|
| 1 | 技術構成 | **`/tech` に移す**（`apps/landing/tech.html`）。中身は今の `index.html` の「技術構成」の節をそのまま（文言・リンク・`docs/decisions.md` へのリンク・GitHub のリンク）。`<title>` は「Nisoine の技術構成」。フッターの「技術構成」からだけ辿れる。`sitemap.xml` には足さない | `/` から面接官向けの話を消す。消さずに移すのは、探せば読める形を残すため（人間の選択） |
| 2 | 価格 | **静的に書く: 月 ¥420・年 ¥4,200**（人間の決定。2026-09-16。Stripe の本番 Price もこの額で作る）。LP に JS を入れない | 048 の「金額は Stripe が真実」はアプリの画面の話。LP は静的で、価格を変えるときは LP も直す（人間が了承） |
| 3 | 写真の上限 | **プレミアム「写真 50 万枚まで」・無料「写真 30 枚まで」**。**055 が先**（アプリ側を 50 万枚にする。LP が先に 50 万と書くと、`/premium` の「5 万枚」と食い違う） | 人間の決定（2026-09-16） |
| 4 | Google Play | **「Google Play は準備中」の 1 行を「はじめる」の近くに**（人間の指示）。App Store は書かない（iOS は段階0 も未着手） | 実装前の店を「準備中」以外の言い方にしない |
| 5 | 「はじめる」 | **主ボタン「はじめる」→ `/app/`**（サインイン画面。Google でログイン）。副リンク「ログインせずにデモを見る」→ `/app/`（同じ先。サインイン画面の「ゲストではじめる」に導く）。ヘッダーの右端と、ヒーローと、プレミアムの節の下と、最後（FAQ の下）の **4 箇所** | 一般向け LP の目的は「はじめる」を押してもらうこと。デモは副 |
| 6 | 見出しの書体 | **明朝をシステムフォントで**: `"Hiragino Mincho ProN", "Yu Mincho", "BIZ UDPMincho", "Noto Serif CJK JP", serif`。本文は `architecture.md` 7節の `fontFamily.ja`。**Web フォントは読み込まない**（015・7節のまま）。数字の「576」も同じ列でよい（Poppins を LP に足さない） | 絵の見出しは明朝。0KB で寄せられる |
| 7 | 色 | `architecture.md` 7節のトークンをそのまま（`bg` `surface` `primary` `brand-ink` `text` `text-muted` `border` `primary-subtle`）。**AI まとめの帯だけ濃紺**（`ai-network.jpg` の色。`#1B1F3B` 前後。B が写真から取る） | LP とアプリの色を揃える。濃紺は写真の地なので例外 |
| 8 | 画像 | `docs/sample/landing/` から `apps/landing/assets/` に **役割の名前**で（`README.md` の作法）。**JPEG 品質 78〜82・表示幅の 2 倍まで**（ヒーローは幅 1552 のまま可）。**1 枚 250KB 以下・合計 1.5MB 以下**。ヒーロー以外は `loading="lazy"`。すべて `width`/`height` を書く。`alt` は装飾なら空 | 初回表示を速く保つ（015）。写真 10 枚を原寸で置くと 5MB を超える |
| 9 | 統計カード（「基本機能は無料」の下） | **HTML/CSS で組む**（写真ではなく）。丸いアイコン 2 つは `docs/sample/プロフィール画像/woman1.jpg`・`man1.jpg`（デモペアと同じ顔。AI 生成）を 120px の円に。名前は「ゆい & れん」（デモペア）。数字は **576 日目・会った日数 94 日**（例。動かない） | 絵の通り。JS を入れずに「例」として見せる |
| 10 | ホワイトモード | LP には作らない。ピンク（`bg` の地）だけ | 外観の切り替えはアプリの中の話 |
| 11 | 横幅 | 本文の最大幅 **960px**（アプリの 640 より広い。写真を並べるため）。カードの角丸 20（`radius.card`）・影は `shadow.card` | 絵は PC 幅で 3 列。スマホは 1 列に畳む |
| 12 | OGP・メタ | `description` を 3 節の文に差し替え。`og:image` は今の `ogp.png` のまま。`canonical`・`theme-color`・favicon はそのまま | 絵は変わっても看板は同じ |
| 13 | CSP | **inline script なし**。CSS は `style.css` に（`style` 属性を使わない）（今と同じ。`security-requirements.md` 7節「ランディングには inline script が無い」を守る）。FAQ の開閉は `<details>` | JS を入れない |
| 14 | `style.css` | 書き直してよい。`privacy.html`・`terms.html`・`tokushoho.html`・`tech.html` も同じ `style.css` を読むので、**法務ページの見た目が崩れないこと**（T5） | 4 ページで 1 つの CSS |
| 15 | 絵の「ある 1 日」の節 | **入れる**（文言は 3 節 #8）。絵の丸いアイコンは `packages/ui/assets/panel-*.png`（96px の線画）を使う。新しい絵は描かない | 機能の説明を「1 日の流れ」で繰り返す。全部ある機能 |
| 16 | 「みんなの声」 | **入れない**（声が無い） | 作った声を載せない |
| 17 | リリース履歴 | 入れない（LP は機能ではない） | 053 と同じ |

## 1. 構成（上から。絵 `mockup-full.jpg` に対応）

| # | 節 | 中身 | 写真 |
|---|---|---|---|
| H | ヘッダー | 左にロゴ（`logo.png`）。右に「特徴」「プレミアム」「はじめる」（ページ内リンク 2 つ + `/app/`）。スマホでは「はじめる」だけ残す | — |
| 1 | ヒーロー | 見出し・副文・機能のピル 4 つ・**「はじめる」**（主）・「ログインせずにデモを見る」（副）・「今はブラウザで使えます。Google Play は準備中です」 | `hero-beach.jpg`（横長。下端を `bg` にぼかして溶かす） |
| 2 | 基本機能は無料 | 小さな見出し + 統計カード（0節 #9） | `woman1.jpg` `man1.jpg` |
| 3 | ふたりのこと、置き場がほしかった。 | 3 枚のカード（見出し・2 行・写真。写真の四方に余白。059） | `phone-chat.jpg` `phone-photos.jpg` `calendar-desk.jpg` |
| 4 | ぜんぶ、Nisoine に。（`id="features"`） | 2 列 × 3 行の 6 つ（アイコン + 名前 + 2 行） | `panel-*.png`（線画） |
| 5 | AI まとめ | 濃紺の帯。中央に文字だけ（059） | `ai-network.jpg`（地） |
| 6 | 気分の記録 / ほしいもの | 2 枚並び。左は折れ線を CSS（`border`）か小さな SVG で。右は写真 | `want-grid.jpg` |
| 7 | 思い出・アルバム | 2 枚並び。左「思い出」右「アルバム」 | `polaroid-softcream.jpg` `polaroid-three.jpg` |
| 8 | Nisoine と過ごす、ある 1 日 | 横 5 列（スマホは縦）。時刻・見出し・1 行 | `panel-*.png` |
| 9 | プレミアム（`id="premium"`） | 月額・年額の 2 枚 + できること 2 行 + 「いつでも解約できます」+ 「プレミアムを見る →」（`/app/premium`）+ **「はじめる」** | — |
| 10 | ふたりのことだから、誰にも見せない。 | 3 つ（1 行ずつ） | — |
| 11 | よくある質問 | `<details>` 4 つ | — |
| 12 | 最後の呼びかけ | ロゴ・タグライン・**「はじめる」** | — |
| F | フッター | 「プライバシーポリシー・利用規約・特定商取引法に基づく表記・技術構成」 | — |

## 2. `/tech`

- `apps/landing/tech.html`。今の `index.html` の `<section class="tech">` を丸ごと移す（文言を変えない。**実装と食い違う記述があれば A に知らせる**。直すのは A）
- `<title>Nisoine の技術構成</title>`。`description` は「Nisoine の技術構成と設計上の判断」。`canonical` は `https://nisoine.com/tech`
- 上にロゴ（`/` へのリンク）、下にフッター（`index.html` と同じ）
- ルーティングは触らない（Static Assets の `html_handling` が `/tech` → `tech.html` を解く。`/privacy` と同じ）。`sitemap.xml` に**足さない**。`robots.txt` は触らない

## 3. 文言（B はこの通りに。ここに無い売り文句を足さない）

### メタ

- `<title>`: `Nisoine - ふたり専用SNS`（今のまま）
- `description`: `Nisoineは、ふたりだけの毎日を残す専用SNS。タイムライン・カレンダー・思い出・アルバム・AIまとめ。基本機能は無料。ブラウザですぐに使えます。`
- `og:description` / `twitter:description`: `ふたりだけの毎日を残す専用SNS。基本機能は無料、ブラウザですぐに使えます。`

### 1 ヒーロー

- 見出し: `ふたりの毎日を、` / `ひとつの場所に。`（2 行）
- 副文: `今日の記録も、記念日も、AIまとめも —` / `ふたりだけの、小さな世界。`
- ピル: `タイムライン` `カレンダー` `思い出` `AIまとめ`
- 主ボタン: `はじめる`（→ `/app/`）
- 副リンク: `ログインせずにデモを見る`（→ `/app/`）
- 注: `今はブラウザで使えます。Google Play は準備中です。`

### 2 基本機能は無料

- 小見出し: `基本機能は無料`
- カード: `ゆい & れん` / `付き合って` `576` `日目` / `会った日数: 94 日`

### 3 ふたりのこと、置き場がほしかった。

| 見出し | 本文 | 写真 |
|---|---|---|
| `今日のことを、残せる` | `写真といっしょに、` / `ふたりのタイムラインに。` | `phone-chat.jpg` |
| `SNS よりも、ふたりだけ` | `他の人には見えない、` / `ふたり専用の場所。` | `phone-photos.jpg` |
| `記念日・予定を忘れない` | `記念日も、これからの予定も、` / `ひとつのカレンダーに。` | `calendar-desk.jpg` |

### 4 ぜんぶ、Nisoine に。

| アイコン | 名前 | 本文 |
|---|---|---|
| `tab-timeline.png` | `タイムライン` | `今日あったことを、写真と言葉で。` / `相手の投稿にはリアクションを。` |
| `tab-calendar.png` | `カレンダー` | `記念日・予定・会った日を、` / `ひとつの月表示で。` |
| `panel-memory.png` | `思い出` | `1ヶ月前、半年前、1年前。` / `過去の投稿をそっと差し出します。` |
| `panel-album.png` | `アルバム` | `旅行やデートの写真を、` / `まとめて残せます。` |
| `panel-list.png` | `行きたい・食べたいリスト` | `「行きたい」「食べたい」を貯めて、` / `行ったらチェック。` |
| `panel-want.png` | `ほしいもの` | `「こういうのが欲しい」を、` / `そっと相手に伝えられます。` |

### 5 AI まとめ

- 見出し: `AIまとめ`
- 本文: `ふたりの 1 週間と 1 ヶ月を、AI が短く振り返ります。`

### 6 気分の記録 / ほしいもの

- 左: `気分の記録` / `1 日 1 回、今日の気分を 1 タップ。` / `ふたり分が並んで見えます。`
- 右: `ほしいものリスト` / `写真と URL で、欲しいものを共有。`（`want-grid.jpg`）

### 7 思い出 / アルバム

- 左: `思い出` / `使い始めた翌日から、過去の投稿がそっと届きます。`（`polaroid-softcream.jpg`）
- 右: `アルバム` / `旅行ごと、季節ごとに、写真をまとめて。`（`polaroid-three.jpg`）

### 8 Nisoine と過ごす、ある 1 日

| 時刻 | 見出し | 1 行 | アイコン |
|---|---|---|---|
| `朝 7:30` | `今日の気分` | `起きたら 1 タップ。` | `panel-mood.png` |
| `昼 12:10` | `行きたい場所を追加` | `ランチで見つけたお店を、リストに。` | `panel-list.png` |
| `夕方 16:40` | `写真を投稿` | `帰り道の空を、タイムラインに。` | `tab-timeline.png` |
| `夜 21:00` | `相手の投稿にリアクション` | `今日もおつかれさま。` | `tab-home.png` |
| `寝る前 23:30` | `1 年前の今日` | `思い出が、そっと届く。` | `panel-memory.png` |

### 9 プレミアム

- 見出し: `プレミアムで、もっと特別なふたりに。`
- 月額のカード: `月額プラン` / `¥420` `/ 月`
- 年額のカード: `年額プラン` / `¥4,200` `/ 年` / バッジ `2 ヶ月分お得`
- できること: `写真を 50 万枚まで保存`（無料プランは 30 枚まで）/ `月額と年額から選べます`
- 注: `いつでも解約できます。自動更新。`
- リンク: `プレミアムを見る →`（→ `/app/premium`）
- ボタン: `はじめる`（→ `/app/`）

### 10 ふたりのことだから、誰にも見せない。

- `ふたり以外には見えません`
- `通信は暗号化されています`
- `いつでも退会でき、データは消えます`

### 11 よくある質問

| Q | A |
|---|---|
| `基本機能は無料ですか？` | `はい。タイムライン・カレンダー・思い出・アルバム（写真 30 枚まで）・AIまとめは無料で使えます。` |
| `プレミアムはいつでも解約できますか？` | `はい。解約後も期間の終わりまで使えます。` |
| `ログインには何が必要ですか？` | `Google アカウントだけです。相手は 6 桁の招待コードで参加します。` |
| `データは安全ですか？` | `ふたりのペア以外からは見えない設計です。写真はいつでも ZIP でまとめて持ち出せます。` |

### 12 最後の呼びかけ

- ロゴ + `日々が、ふたりの記録になる。` + `はじめる`

### F フッター

- `プライバシーポリシー ・ 利用規約 ・ 特定商取引法に基づく表記 ・ 技術構成`（`/privacy` `/terms` `/tokushoho` `/tech`）

## 4. B の作業

- `docs/sample/landing/` → `apps/landing/assets/`（0節 #8。加工の手順は `artifacts/054/scripts/` に残す）
- `index.html`・`style.css` を書き直す。`tech.html` を新設（2 節）
- `sitemap.xml` は触らない（`/tech` を足さない）

## 5. テストで証明すること

| # | 何を | どこで |
|---|---|---|
| T1 | `GET /` `GET /tech` が 200 で、`/` に「技術構成」の節が無く、`/tech` にある | `apps/api` |
| T2 | `/` の HTML に `<script` が無い（CSP のハッシュが要らないまま） | `apps/api` |
| T3 | `/` の `<img>` 全部に `width` と `height` がある | `apps/api` |
| T4 | `apps/landing/assets/` の JPEG が 1 枚 250KB 以下・合計 1.5MB 以下 | `scripts` か `apps/api` |
| T5 | `/privacy` `/terms` `/tokushoho` が 200（`style.css` の書き直しで壊れていない） | `apps/api` |
| T6 | `/` の応答ヘッダ（CSP 等）が 053 の T4b と同じ値 | `apps/api`（既存のテストがそのまま緑） |

## 完了条件

- T1〜T6。`pnpm -r test`・型チェック・lint
- **375px 幅で横スクロールが無い・960px 幅で絵に近い**（`wrangler dev` の画面キャプチャを `artifacts/054/` に。PC・スマホ各 1 枚）
- 本番デプロイ後、人間が `https://nisoine.com/` と `/tech` を見る
- `state.md` / `worklog.md`

## 停止条件

- 写真を 250KB に収めると明らかに荒れる → 上限を 350KB に緩めて `state.md` に書く（A に聞かない）
- 3 節の文言が機能と食い違うと B が気づいた → 直さず A に知らせる
- レビュー往復 3 回 → A へ

## 順序

**055（写真の上限 50 万枚）の後。**047（鍵）・iOS の前。
